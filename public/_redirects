# Default-language landing for the bare root path. Avoids "/" and "/zh/"
# returning the same canonical-mapped HTML to crawlers.
/  /zh/  302

# SPA fallback for client-side routing
/*  /index.html  200
